print("hello world, R is super hard. Don't worry, it gets easier!")
# git branch mikki # mikki is the name of the branch
# from command line
# git checkout branchname

# push a local branch to the master branch (origin)
# git push -u origin mikki